@* <!doctype html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>OTP Verification</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --card-radius: 18px
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg,#f7f9ff,#e8f0ff);
            font-family: system-ui,Segoe UI,Roboto,"Helvetica Neue",Arial
        }

        .otp-card {
            border-radius: var(--card-radius);
            box-shadow: 0 10px 30px rgba(33,37,41,.08);
            overflow: hidden
        }

        .brand-strip {
            background: linear-gradient(90deg,#6c63ff,#4f8ef7);
            color: #fff;
            padding: 28px
        }

            .brand-strip h3 {
                margin: 0;
                font-weight: 700
            }

        .otp-input {
            width: 56px;
            height: 64px;
            border-radius: 12px;
            border: 1px solid #e6e9ef;
            text-align: center;
            font-size: 24px;
            font-weight: 600
        }

            .otp-input:focus {
                box-shadow: 0 0 0 4px rgba(79,142,247,.12);
                border-color: #4f8ef7
            }

        .muted-small {
            font-size: .92rem;
            color: #6b7280
        }

        .timer {
            font-weight: 700;
            color: #374151
        }
        @media (max-width:420px) {
            .otp-input

        {
            width: 46px;
            height: 56px;
            font-size: 20px
        }

        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-12 col-sm-10 col-md-8 col-lg-5">
                <div class="card otp-card">
                    <div class="brand-strip d-flex align-items-center justify-content-between">
                        <div>
                            <h3>التحقق برمز التحقق</h3>
                            <div class="muted-small">أدخل الكود المرسل إلى رقمك أو بريدك</div>
                        </div>
                        <div class="text-end">
                            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
                                <rect width="24" height="24" rx="6" fill="rgba(255,255,255,.12)" />
                                <path d="M7 13l3 3 7-8" stroke="#fff" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" />
                            </svg>
                        </div>
                    </div>
                    <div class="card-body p-4">
                        <p class="mb-2">رمز مكون من 6 أرقام تم إرساله إلى <strong>+20 1XX XXX XXXX</strong></p>
                        <form id="otpForm" class="text-center">
                            <div class="d-flex justify-content-center gap-2 my-3" id="otpInputs" role="group" aria-label="OTP digits">
                                <input inputmode="numeric" maxlength="1" class="form-control otp-input" id="d1" autocomplete="one-time-code" />
                                <input inputmode="numeric" maxlength="1" class="form-control otp-input" id="d2" autocomplete="one-time-code" />
                                <input inputmode="numeric" maxlength="1" class="form-control otp-input" id="d3" autocomplete="one-time-code" />
                                <input inputmode="numeric" maxlength="1" class="form-control otp-input" id="d4" autocomplete="one-time-code" />
                                <input inputmode="numeric" maxlength="1" class="form-control otp-input" id="d5" autocomplete="one-time-code" />
                                <input inputmode="numeric" maxlength="1" class="form-control otp-input" id="d6" autocomplete="one-time-code" />
                            </div>

                            <div class="d-flex justify-content-between align-items-center mb-3 px-3">
                                <div class="muted-small">لم تستلم الكود؟</div>
                                <div class="d-flex align-items-center gap-2">
                                    <div class="timer" id="countdown">00:30</div>
                                    <button type="button" id="resendBtn" class="btn btn-link p-0" disabled>إعادة الإرسال</button>
                                </div>
                            </div>

                            <div class="d-grid mb-2 px-3">
                                <button class="btn btn-primary btn-lg" id="submitBtn" type="submit">تأكيد</button>
                            </div>

                            <div class="px-3 text-center">
                                <button type="button" class="btn btn-outline-secondary btn-sm" id="editBtn">تغيير الرقم</button>
                            </div>
                        </form>

                        <div class="alert alert-success mt-3 d-none" id="successMsg">تم التأكيد بنجاح — جاري التحويل...</div>
                        <div class="alert alert-danger mt-3 d-none" id="errorMsg">الكود غير صحيح. حاول مرة أخرى.</div>
                    </div>
                </div>

                <p class="text-center text-muted mt-3 small">نسخة تجريبية — يمكنك تخصيص النصوص والألوان</p>
            </div>
        </div>
    </div>

    <script>
        // helpers
        const inputs = Array.from(document.querySelectorAll('.otp-input'));
        inputs.forEach((input, idx) => {
          input.addEventListener('input', (e) => {
            const val = e.target.value.replace(/[^0-9]/g, '');
            e.target.value = val;
            if (val && idx < inputs.length - 1) inputs[idx + 1].focus();
          });
          input.addEventListener('keydown', (e) => {
            if (e.key === 'Backspace' && !e.target.value && idx > 0) {
              inputs[idx - 1].focus();
            }
          });
          // allow paste of full code
          input.addEventListener('paste', (e) => {
            e.preventDefault();
            const paste = (e.clipboardData || window.clipboardData).getData('text');
            const digits = paste.replace(/\D/g, '').slice(0, inputs.length).split('');
            digits.forEach((d, i) => inputs[i].value = d);
            if (digits.length) inputs[Math.min(digits.length, inputs.length)-1].focus();
          });
        });

        // countdown & resend
        let countdownSec = 30;
        const countdownEl = document.getElementById('countdown');
        const resendBtn = document.getElementById('resendBtn');

        const tick = () => {
          const mm = String(Math.floor(countdownSec / 60)).padStart(2,'0');
          const ss = String(countdownSec % 60).padStart(2,'0');
          countdownEl.textContent = `${mm}:${ss}`;
          if (countdownSec === 0) {
            resendBtn.disabled = false;
            resendBtn.classList.remove('disabled');
            countdownEl.textContent = 'انتهى الوقت';
            return clearInterval(timer);
          }
          countdownSec--;
        }
        tick();
        const timer = setInterval(tick, 1000);

        resendBtn.addEventListener('click', () => {
          // محاكاة إعادة الإرسال
          resendBtn.disabled = true;
          countdownSec = 30; // reset
          countdownEl.textContent = '00:30';
          resendBtn.classList.add('disabled');
          // في تطبيق حقيقي: اطلب من السيرفر إعادة إرسال الكود
          // هنا نعيد تشغيل العداد
          clearInterval(timer);
          setInterval(tick, 1000);
          alert('تم إرسال كود جديد (تجريبياً)');
        });

        // form submit
        document.getElementById('otpForm').addEventListener('submit', (e) => {
          e.preventDefault();
          const code = inputs.map(i => i.value || '').join('');
          if (code.length < inputs.length) {
            showError('من فضلك أدخل كل الأرقام.');
            return;
          }
          // محاكاة تحقق
          // في الواقع—أرسل `code` إلى السيرفر عبر fetch
          if (code === '123456') {
            showSuccess();
          } else {
            showError('الكود غير صحيح.');
          }
        });

        function showSuccess(){
          document.getElementById('successMsg').classList.remove('d-none');
          document.getElementById('errorMsg').classList.add('d-none');
        }
        function showError(msg){
          const el = document.getElementById('errorMsg');
          el.textContent = msg;
          el.classList.remove('d-none');
          document.getElementById('successMsg').classList.add('d-none');
        }

        document.getElementById('editBtn').addEventListener('click', () => {
          alert('انتقل لشاشة تغيير الرقم (تخصيص)');
        });

        // focus first input on load
        window.addEventListener('load', () => inputs[0].focus());
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
 *@